# Production environment overrides
server:
  host: "0.0.0.0"
  port: 8000
  workers: 8
  max_concurrent_requests: 5000

logging:
  level: "INFO"
  format: "json"
  telemetry_enabled: true

bedrock:
  timeout: 90
  max_retries: 5
